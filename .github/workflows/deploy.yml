name: Deploy to Shared Hosting (cPanel)

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐘 Setup PHP 8.4
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, iconv, intl, pdo_mysql, bcmath, zip, sqlite3, pdo_sqlite
          coverage: none

      - name: 📦 Install Composer Dependencies (Dev & Test)
        run: |
          composer install --no-interaction --prefer-dist
          cp .env.example .env
          php artisan key:generate

      - name: 🟢 Setup Node.js (LTS)
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: 🎨 Install NPM Dependencies & Build Assets
        run: |
          npm ci --legacy-peer-deps || npm install --legacy-peer-deps
          npm run build

      - name: 🧪 Run Automated Tests
        run: |
          touch database/database.sqlite
          php artisan test --compact

      - name: 📦 Optimize Composer Dependencies for Production
        run: |
          composer install --no-dev --optimize-autoloader --no-interaction --prefer-dist

      - name: 🧹 Prepare Deployment Files
        run: |
          # Remove development-only files
          rm -rf node_modules
          rm -rf tests
          rm -rf .github

      # Deploy via FTP/FTPS to Shared Hosting
      - name: 🚀 Deploy to cPanel via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER || secrets.FTP_HOST || secrets.HOST }}
          username: ${{ secrets.FTP_USERNAME || secrets.FTP_USER || secrets.USER }}
          password: ${{ secrets.FTP_PASSWORD || secrets.FTP_PASS || secrets.PASSWORD }}
          server-dir: ${{ secrets.FTP_TARGET_DIR || secrets.FTP_DIR || '/' }}
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            .env
            storage/logs/*.log
