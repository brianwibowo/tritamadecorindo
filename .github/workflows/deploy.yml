name: Deploy to Shared Hosting (cPanel)

on:
  push:
    branches: [ main ]

env:
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: "true"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐘 Setup PHP 8.4
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, iconv, intl, pdo_mysql, bcmath, zip, sqlite3, pdo_sqlite
          coverage: none

      - name: 📦 Install Composer Dependencies (Dev & Test)
        run: |
          composer install --no-interaction --prefer-dist
          cp .env.example .env
          php artisan key:generate

      - name: 🟢 Setup Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: 🎨 Install NPM Dependencies & Build Assets
        run: |
          npm ci --legacy-peer-deps || npm install --legacy-peer-deps
          npm run build

      - name: 🧪 Run Automated Tests
        run: |
          touch database/database.sqlite
          php artisan test --compact

      - name: 📦 Optimize Composer Dependencies for Production
        run: |
          composer install --no-dev --optimize-autoloader --no-interaction --prefer-dist

      - name: 🧹 Prepare Deployment Files
        run: |
          # Remove development-only files
          rm -rf node_modules
          rm -rf tests
          rm -rf .github

      # Deploy via FTP/FTPS to Shared Hosting (Runs if FTP_SERVER secret is configured)
      - name: 🚀 Deploy to cPanel via FTP
        if: ${{ secrets.FTP_SERVER != '' || secrets.FTP_HOST != '' }}
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER || secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME || secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASSWORD || secrets.FTP_PASS }}
          server-dir: ${{ secrets.FTP_TARGET_DIR || '/' }}
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            .env
            storage/logs/*.log

      - name: ℹ️ FTP Secret Notice
        if: ${{ secrets.FTP_SERVER == '' && secrets.FTP_HOST == '' }}
        run: |
          echo "ℹ️ FTP_SERVER secret belum dikonfigurasi di GitHub Repository Settings -> Secrets -> Actions."
          echo "Build & Test selesai dengan sukses."

